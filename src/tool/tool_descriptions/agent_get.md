Read the current agent-plane summary, including identity visibility, ownership, profile preset, lifecycle, active work focus, waiting state, and visible child-agent lineage. With `agent_id` set, resolve the requested agent through the host bridge and return its summary; private child agents are reachable because the local control API is treated as a trusted operator surface (see `docs/runtime-spec.md` and issue #1742). Errors: `agent_not_found`, `agent_archived`, `agent_stopped`, `host_unexpected_private` (latter indicates a host bug).