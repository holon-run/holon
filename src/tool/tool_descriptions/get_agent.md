Read an agent-plane summary, including identity, lifecycle, active work focus,
waiting state, and child-agent lineage.

When called without arguments, returns the current agent's summary. When called
with `agent_id`, returns the requested agent's canonical detail projection.
Read-only inspection does not start an unloaded target runtime.
