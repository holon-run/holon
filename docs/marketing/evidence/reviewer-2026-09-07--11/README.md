# Reviewer 博客统计核验：2026-09-07 至 2026-09-11

状态：中文博客内部审阅证据，未发布。统计仓库为 `holon-run/holon`。

## 口径与结果

窗口为 **2026-09-07 00:00:00 UTC（含）至 2026-09-12 00:00:00 UTC（不含）**。
以 Reviewer 提供的本地首次审阅操作记录确定候选，再核对公开审阅及 PR 终态；
按独立 PR 去重，不按创建日期、事件、评论或 WorkItem 数量替代。

| 指标 | 结果 | 含义 |
| --- | ---: | --- |
| 窗口内有 Reviewer 审阅记录的独立 PR | 61 | 本地记录确定范围，公开快照确认审阅活动 |
| 窗口内已合并 | 61 | GitHub `mergedAt` 落在同一窗口内，不代表全部由 Reviewer 执行合并 |
| 样本内未合并 | 0 | 未按合并结果筛选样本 |
| 共享账号 `jolestar` 的正式 review | 74 | 窗口内、上述 61 个 PR；不是 Agent 独占的执行次数 |
| 共享账号 `jolestar` 的 issue comment | 42 | 窗口内、上述 61 个 PR；包含跟进说明及其他参与工作的发言 |
| 本地首次操作形式 | 60 review、1 comment | comment 为 #2823；不等于该 PR 在 GitHub 上的首次审阅形式 |

机器可读数据见 [audit.json](audit.json)，逐 PR 表见 [prs.csv](prs.csv)。
运行 `python3 docs/marketing/evidence/reviewer-2026-09-07--11/check.py` 可重算上述数字并核对 CSV。

## 双源核验与修正

- Reviewer 提供 `db_summary.json`、`actions.tsv`、`gh_details.jsonl`、`pr_titles.tsv`。
  四份原始文件的 SHA-256 保存在 `audit.json`；本地汇总与操作表已去掉工具、WorkItem 等内部标识。
- 本地操作表共 **213 条**，覆盖范围宽于统计窗口。窗口内为 69 条 review、16 条 comment、66 条 merge 命令记录；
  它们包含尝试与重试，不是成功操作数，也不等同于公开活动数。
- 独立 GitHub 快照采集于 2026-09-12 16:06:32–16:07:06 UTC，最初按创建日期采集了 81 个候选；
  本地记录确定的 61 个 PR 全部在其中。每个 PR 的 reviews、comments、commits 均无待翻页标记。
  持久化数据保留公开事件 ID、链接、时间和账号，以及原快照哈希；不保留完整正文、命令或日志。
- Reviewer 的 `gh_details.jsonl` 含未转义换行；即使允许控制字符，#2815、#2826、#2834、#2842、
  #2843、#2851、#2889、#2899 仍有转义或拼接损坏。这里不用文本替换猜测原文，
  而是统一采用独立 GitHub 快照生成有效 JSON；原始损坏文件未直接纳入项目。
- **#2823 的时间顺序需要修正。** 公开正式 review 为 2026-09-07 17:18:20 UTC，
  本地首次 comment 操作为 23:33:12.886 UTC，对应公开评论为 23:33:17 UTC。
  本轮还用 GitHub 在线 API 复核了两条记录。因此不能写“先评论，后续才有正式 review”。
  公开 review：[5134269341](https://github.com/holon-run/holon/pull/2823#pullrequestreview-5134269341)；
  对应评论：[5576745644](https://github.com/holon-run/holon/pull/2823#issuecomment-5576745644)。
- **#2862 的首次命令不是成功发布。** DB 首次 review 操作在 2026-09-10 05:46:12.254 UTC，
  批次内该命令返回 HTTP 499、退出码 1；05:58:00.792 UTC 的后续命令成功，
  对应公开 review 时间为 05:58:04 UTC。窗口归属不变，但不能把首次尝试时间当作首次发布成功时间。

`nearest_public_event` 仅是同账号、同操作形式、时间最接近的公开记录，用于定位核验，
不是自动完成执行者归因的判定。CSV 因此分别保留本地首次尝试时间和对应的公开时间。

## 尚未证明的事情

- “首审无阻塞后合并”“反馈修复并复查后合并”尚未逐 PR 分类；
  CSV 全部标为未分类，不把 review 次数或评论存在与否当作修复证据。
- 尚未逐条核对成功执行合并的回执；本地 merge 命令或 GitHub 的共享合并账号不足以单独证明执行者。
- 操作者提供了 61 个 PR 均有 WorkItem 的既有核验结论，但本轮四份输入不含完整 WorkItem 映射，
  未独立复验该不变量，也未把内部标识纳入项目。
- 74/42 是共享账号口径；不能据此声称没有其他 Agent 或人类参与。
  首次记录之前是否存在未纳入本地汇总的审阅，依赖 Reviewer 提供的历史覆盖说明，
  本轮没有重新扫描完整运行数据库。
- 这是单仓库、五个完整 UTC 日的使用记录，不是质量基准测试。
  全部合并不说明没有漏报、误报，也不能推导缺陷修复率、节省时间或其他仓库的预期效果。
