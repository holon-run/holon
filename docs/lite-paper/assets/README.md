# Lite paper assets

Keep shared diagram sources and necessary images here. Page previews, PDF
outputs and rendering diagnostics belong under `build/lite-paper/`, not here.

The intermediate renderer preserves the current Markdown text diagrams. Vector
diagram layouts can be added to `tools/layout.py` later; their labels and product
copy must remain sourced from the language Markdown.
