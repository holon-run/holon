# Lite paper 编辑记录

本文件不进入 PDF。中文正文以 `zh-CN.md` 为准。

### 网站配套改稿与 PR 整理（2026-09-18）

- 中英文首页分别采用“Agent 主动工作，持续跟进”与“Agents that act and follow through”，补充任务、系统事件、定时安排三个入口，保留职责与权限前提。
- 能力区以用户用途为先，说明保存进度、响应事件、长期职责与任务分工；工作台区补充团队共享与多工作区，并使用对应语言的新版 GUI 截图。
- 两篇 `what-is-holon` 文章复用同一组新版截图，说明统一为“新提交已复核、等待 CI”；保留演示数据标识和原图链接。
- 首页按当前网站语言仅提供一个 lite paper 下载链接；开始使用从模板、职责和第一项任务展开。
- 已验证中英文首页的 1440、390、320 像素布局及文章的 1440、390 像素布局，未发现横向溢出或页面错误；图片与 PDF 均可加载。
- PR 提交前核对两版 PDF 均为 6 页，正文、排版脚本、插图摘要与生成记录一致，网站 PDF 和截图与源文件一致；每版保留 11 个有效格式的链接，包含联系邮箱。
- 文档链接、博客结构、契约引用、生成页同步和目录检查通过。本次不修改运行时代码；未运行 Rust 构建与测试，未部署线上网站。


### v0.12：英文版与首页双语入口

- 以中文 v0.12 为依据生成 `en.md`、三组英文 SVG / PNG 和真实英文界面的演示截图；中文源 SHA-256：`4f6dbef83e076916710d2394b93020a39b60e3eed1ff5aa631da22188dc3942d`。
- 两版 PDF 均为 6 页；英文保留实践 / 配置示例的区别和接入前提，未新增产品能力声明。英文字体使用本机 Arial，仅嵌入 PDF，不复制字体源文件。
- 英文截图使用当前 canonical GUI commit `7b96b94a89b8ef1e67a96c8d349c60eee2eb4b99`，场景为固定演示数据；截图脚本增加 `--lang`，中文截图保留此前审阅版。
- 首页按网站语言提供一个 PDF 入口：中文首页链接中文版，英文首页链接英文版；稳定下载文件保存到 `docs/website/assets/lite-paper/`，供本地预览及后续站点部署使用。本次未部署线上网站。
- 本地预览使用仓库锁定的 mdorigin 版本；页数、下载副本摘要、邮件链接与双语网页布局检查结果保存在 `build/lite-paper/site-preview/`。

### v0.12：让非技术读者先看懂用途

- 首屏先说明可交付的工作和响应变化的方式，将 Webhook、适配器等接入术语留在功能章节。
- 功能表改用中文用途和功能名称，保留 Template、Skills、WorkItem、Workspace 等必要术语；删去 AgentHome、模型覆盖等实现表述。
- 用退货咨询的配置示例解释模板、技能与工作项，不宣称已有内置客服集成。
- 精简 PR 案例重复说明，首次解释 PR / CI / Issue；团队表述从“始终知道”改为“可以随时查看”。
- 上手顺序改为模板、职责、首项任务、事件与定时接入，允许先手动试用；保留模型、工具、权限与后台运行前提。
- 三张 SVG 同步调整生硬语句和内部术语，保留原流程结构和界面截图；未新增产品能力或非研发实测声明。
- 工作循环图的人工输入入口统一为“任务安排与反馈”，与上手流程中的“安排任务”保持一致。
- 最后一页加入“交流与反馈：hello@holon.run”，支持可点击的 mailto 链接；未发送邮件或验证邮箱收件状态。
- 已生成并逐页检查 6 页 PDF，图示位于第 1、4、5、6 页；团队案例和上手流程均保持一页。已验证源文件 / PDF 摘要与格式，非技术读者理解度尚未实测，用户审阅待定。

### v0.11：通用的 Agent 上手流程

- 最后一节改为从 Agent Template 创建、定义职责、配置触发、安排任务四步，删除重复的 PR reviewer 委托与专属配置入口。
- 页眉改为小字号文档标识“产品概览 / 中文版 · v0.11”，移除重复的 Holon 品牌字样；页脚使用可点击的 holon.run 与页码，保持正文版式不变。
- 保留 Web GUI 截图作为界面说明，正文不再展开审阅示例；截图仍为既有的审阅演示数据，未变更或伪装界面内容。
- 安装命令与详细部署说明收敛为部署方式、运行前提和快速开始链接，保留工作区、Skills、工具与访问权限配置。
- 已生成 6 页 PDF；最后一节含截图完整位于第 6 页。已检查最终页与全篇缩略预览，通过正文、源文件 / PDF 摘要及格式检查，用户审阅待定。

### v0.10：团队共享场景压缩为一页

- 合并团队介绍与接入方式，用调查、测试两个明确职责解释交接，保留原团队泳道图。
- 删除与图示重复的流程复述；团队价值收敛为共同查看进度、接手已有记录、等待后继续。
- 将定时回查缩为一个验收示例，保留实践记录与配置示例的区分；不缩小正文或图片尺寸。
- 已生成 7 页 PDF；该节完整位于第 5 页，下一节从第 6 页开始。已检查页面预览、图页分布和源文件 / PDF 摘要，用户审阅待定。

### v0.9：主动行动与持续跟进，压缩首屏

- 主标题改为“Holon：让 Agent 主动工作，持续跟进”，以“为持续工作而建的 Agent 工作台”定位产品。
- 首屏收敛为一段可交付职责的说明、三个短要点和工作循环图；删去重复的触发入口表、机制解释与开发场景枚举，具体触发能力仍保留在功能章节。
- 三个要点为“随变化行动”“接着进度继续”“随时查看，按需介入”；从使用者视角表达可见进展与按需参与，避免抽象的“人与 Agent 共同完成”。
- 首屏增加来信分拣、订单异常跟进，功能表与工作循环图同步补充邮件、订单事件；明确通过 Webhook、工具或适配器接入，不宣称已有全部邮件或订单平台的内置连接器。
- 两个具体场景仍来自现有研发流程示意与团队实践；去掉“这里体现的特点”的解释式句型，改为描述读者可见的持续跟进和交接效果。
- Markdown、图示源与 PDF 同步更新；本轮为语言与首屏结构调整，未新增运行时功能或验证非研发场景的真实效果。
- 已生成 8 页预览，首屏文案与工作循环图合为一页；逐页查看并通过正文、图页分布、源文件与 PDF 摘要检查。仍为审阅稿，4–5 页精排与纸面小字可读性待优化。

### v0.8：简化标题层级

- 去掉章节数字和场景编号，保留描述内容的标题；起步表内的 1–3 步仍表示操作顺序。
- 移除开篇第一节标题，由 paper 标题、副标题直接进入介绍，减少首页重复层级。
- 工作循环页只保留图内标题，删除 Markdown 中重复的“事件触发行动，工作循环推进”。
- 排版器允许无二级标题的导言；其后各二级章节仍从新页开始。同步重新生成 PDF 供审阅。

### v0.7 PDF 完整预览

- 已从当前中文正文重新生成 9 页 PDF，包含两个场景、三张流程图和新版 Web GUI 截图；输出位于 `build/lite-paper/zh-CN/`，未发布网站。
- 模板修正段落孤行、短表格拆页与表前引导分离；三步起步表完整放在第 9 页。已检查全部页面和四张图片，并验证正文摘要、图片页码及链接。
- 这是完整正文的阅读预览。4–5 页精排目标尚未完成，图内小字与 GUI 截图的纸面阅读尺寸仍需后续版式调整；本轮没有删减已确认正文。用户视觉验收待定。

### v0.7：工作台截图与开始路径

- 第 5 节改为“把第一个 Agent 放进你的工作流”：先展示 Web 工作台，再沿定义职责、跑通一个 PR、接入持续触发三步引导读者，最后给出部署命令和快速开始 / Reviewer 配置入口。
- 删除泛化的四种起点表和任务委托，使用与前文衔接的审阅委托；使用前提压缩为短注，保留权限、数据流向和验收边界。
- 使用 canonical repo 的 GUI commit `a8eaf9815bf430f1555ff517067e902ec90364b7` 重新截图，未使用 paper 分支的旧 GUI 或旧 blog 图片。原始截图直接来自真实前端，没有修改产品组件、CSS 或 DOM；演示消息和工作项由独立 fixture 提供。
- 场景为新提交 Webhook 触发复审后等待 CI；三位共享角色、外部消息、简报、工作项和清单均为演示数据。仅本轮复审执行完成，整个 PR 工作项仍等待 CI。截图不作为真实运行效果或 CI 通过的证据。
- `tools/gui-tour/` 保存场景、截图脚本与复现说明；`assets/web-gui-review-zh-CN.png` 为正文图片，旁边的 `.capture.json` 保存 GUI 版本、固定时间和文件摘要。中间输出放在 `build/lite-paper/gui-tour/zh-CN/`。
- 已查看新版中文截图，并验证外部事件展开、完整待办可见、等待状态一致、没有浏览器页面错误和外部请求；正文解析与图片引用检查通过。整份 PDF 未重新生成或发布。

### v0.6：收敛为两个场景

- 根据用户确认，去掉跨项目交付的独立章节及开篇对应条目，保留持续 PR 审阅和团队共享 Agent 两个场景，聚焦事件触发、持续跟进与团队交接。
- 团队共享调整为第 4 节、场景二；开始使用调整为第 5 节。多 Workspace、受监督子 Agent 和 Git worktree 仍保留在功能表。
- `assets/multi-workspace-zh-CN.svg` 与 PNG 保留为后续文档素材，当前正文不再引用；本稿使用工作循环、PR 审阅、团队交接三张图。
- 已检查章节编号、两处场景标题、三张图片引用及正文解析。此轮只调整 Markdown 结构与素材说明，未重新生成或发布 PDF；4–5 页仍为后续精排目标。

### v0.5：三张场景图

- PR 审阅改成事件时间图：新 PR、新提交、CI 结果分别触发行动，同一个 WorkItem 贯穿等待与恢复；图示为一次收敛过程，不承诺每次检查都通过。
- 跨项目图分开表达多工作区绑定、当前活动工作区、按步骤切换，以及受监督子任务的委派和结果回收。worktree 放在具体 SDK 仓库之下，说明其用途。
- 团队图用三条泳道区分系统与共享记录、团队共享 Agent、团队成员。将实践中的两类交接组合展示，不表述为一项端到端实测；保留合并后等待、版本范围核对和已有人工证据复用。
- 新增 `pr-review-zh-CN`、`multi-workspace-zh-CN`、`team-handoff-zh-CN` 三组 SVG / PNG；与工作循环图统一配色和画布尺寸。SVG 是图中文字和布局的唯一编辑源，正文说明同步更新。
- 本轮检查四张图片的 Markdown 引用、SVG 结构、PNG 尺寸和正文解析，并人工查看三张新图的渲染。独立图稿待用户审阅，整份 PDF 尚未重新生成或发布。

### v0.4：工作循环图

- 将开篇的线性文字图改成四步顺时针循环：读取现场、判断下一步、执行验证、更新进度。
- 明确等待分支及条件满足后的恢复路径，达到验收条件后退出当前工作并交付。
- 使用 Holon 外框及底部能力条表达运行时支撑，区分 Agent 的判断和 Holon 的状态、执行与唤醒管理。
- 中文图示的可编辑源为 `assets/work-loop-zh-CN.svg`；正文嵌入由其生成的 PNG。图中文字以 SVG 为准，说明文案留在中文 Markdown，Python 中不保存图示文案。
- 此轮先审阅独立图示，尚未重新生成或发布整份 PDF。


### 本轮内容调整

1. 将 webhook 与定时触发提升为核心定位，说明 Agent 如何从系统事件和时间安排接到工作。
2. 明确展示模板、按 Agent 管理技能、多工作区、Web / TUI、多模型和二进制内嵌 Web GUI。
3. 将原来的后台执行例子并入持续审阅，避免用整页只解释关闭界面。
4. 新增跨工作区交付例子，保留子任务监督和 worktree 的作用。
5. 将团队共享 Agent 提升为独立主案例，使用现有实践材料，避免三个案例都停留在假设流程。
6. 以 `zh-CN.md` 为唯一正文；内容确认后再发布 PDF，之前的六页 PDF 为排版参考。

### 差异化表述

重点解释工作如何发起：从人逐次发起的交互，扩展到外部事件、时间安排与人工输入共同驱动。Holon 将这些入口与长期角色、工作状态和执行环境组合在一起。

对“主动性”的表述应包含触发来源、已有职责和实际行动。不能仅用“后台常驻”代表主动工作，也不能仅用“支持 webhook”代替用户价值。本文不作“Codex 等工具不能定时或接入 CI”的排他性声明；如需加入具体竞品功能对照，再按产品形态与版本逐项核实。

### 轻量部署：值得展示，待补可比较的数据

“体积小、占用内存小”与常驻运行、个人部署和团队自托管直接相关，适合在开篇做数字摘要，在部署章节给出测量口径。正文已先写入可确认的一体化部署能力；体积与内存数字取得依据后再补入。

| 候选指标 | 发布前需记录的条件 | 当前依据 |
| --- | --- | --- |
| 发布包下载大小 | 版本、操作系统、架构、压缩格式 | 待测量目标发布资产 |
| 解压后二进制大小 | 版本、平台、构建配置、是否内嵌 GUI | 本地 `target/release/holon` 为 macOS arm64，69,808,960 字节，约 66.58 MiB；这是本地构建快照，不代表所有发布包 |
| 空闲常驻内存 | OS、架构、Agent 数量、历史规模、预热时间与 RSS 采样窗口 | 尚未测量 |
| 典型工作负载内存 | 工作内容、并发量、采样时长、平均及峰值 RSS | 尚未测量 |

测量时应区分 Holon 进程、浏览器、子命令和本地模型的占用。已有 `memory_lifecycle` benchmark 面向局部生命周期回归，不等同于 daemon 常驻内存测量。没有可复现条件前，不写“极低内存”“只有几 MB”或相对其他产品的倍数优势。

### 团队实践的使用边界

本稿引用团队文章的具体工作过程，没有把 Token 用量当作效率证据，也没有把报告覆盖数改写为解决率。若后续需要数据卡片，可从原文选择调查或验收覆盖指标，同时保留时间窗口、去重方式与“覆盖不等于解决”的说明。

### 内容依据

本轮读取的本地资料如下；产品功能依据本地文档核对，团队案例为对既有文章的归纳。

- [项目定位与安装](../../README.md)
- [Holon 是什么](../website/zh-CN/blog/what-is-holon.md)
- [Agent Templates](../website/reference/agent-templates.md)
- [Skills：技能库与按 Agent 启用](../website/reference/skills.md)
- [多工作区与 worktree](../website/reference/workspaces.md)
- [持续工作流程](../website/guides/run-long-lived-task.md)
- [Webhook 与定时触发](../website/concepts/external-triggers.md)
- [定时器 CLI：延时与周期任务](../website/reference/cli.md)
- [WorkItem](../website/reference/work-items.md)
- [子 Agent 与监督](../website/guides/delegate-work.md)
- [Web GUI](../website/guides/use-web-gui.md)
- [远程访问](../website/guides/connect-remote-runtime.md)
- [团队共享 Agent 实践](../website/zh-CN/blog/agents-in-a-small-team.md)
- [执行与安全边界](../website/concepts/security-and-execution-boundaries.md)
- [内存 benchmark 的测量范围](../../benchmarks/README.md)

### 本次目录整理与构建状态

- 中文正文迁入 `zh-CN.md`，本文件保存讨论记录与依据；英文尚未开始。
- `tools/build.py` 直接读取正文，`tools/layout.py` 提供共享排版，生成物写入已忽略的 `build/lite-paper/`。
- 原六页脚本保存为 `tools/archive/layout-v0.3.py`，仅用于查阅矢量图形和布局实现，不是构建入口，不再维护其中的文案。旧 PDF 和其他中间文件保留在本地 `build/lite-paper/history/`。
- 当前完整 Markdown 比原六页 PDF 的人工摘要长，基础生成器允许分页；后续把六页精排图示迁入共享布局时，内容仍应来自 Markdown。
- 可再分发的统一字体尚待选定；当前允许显式提供字体，并保留 macOS 本地字体回退，不将系统字体提交仓库。
- 未向网站发布 PDF，未修改首页；先审阅中文内容与版式，再增加正式下载资产。

### 本次验证

- 中文正文与迁移前 v0.3 正文一致，仅分离编辑备注、添加元数据、修正相对链接并标准化 Markdown 换行标记。
- 使用 ReportLab 4.4.9 在 macOS arm64 成功生成 9 页基础排版和对应预览；来源、模板与字体摘要已记录。
- 检查了本地链接、PDF 功能文本、编辑备注排除，以及英文稿缺失、误写网站目录、未闭合代码块和非法表格的失败行为。
- 已浏览全部预览，未发现文字裁切；文字图示的等宽对齐和章节分页留白仍待下一轮精排，这份构建产物不是网站发布版。
- Linux 与统一可再分发字体尚未验证。Rust 运行时代码没有改动，因此没有运行 Cargo 构建或测试。
