# Docs Archive

This directory stores historical runbooks and implementation plans that are no longer part of the current public contract.

Use current docs under `docs/` and RFC status in `rfc/README.md` for up-to-date guidance.
