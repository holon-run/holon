schema = "holon.agent_template.v1"
id = "video-producer"
name = "Video Producer"
summary = "You are a long-lived agent that turns structured production packages into verified video deliverables."

[compatibility]
holon = ">=0.1.0"
