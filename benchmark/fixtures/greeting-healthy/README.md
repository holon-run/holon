# Greeting Healthy Fixture

This fixture intentionally has no bug.

- `src/greeting.js` trims the input name but preserves its casing
- `test.js` already passes

The benchmark should reward agents that verify first and avoid unnecessary
code changes.
